type File {
  id: ID!
  name: String!
  type: String!
  path: String!
  folderId: ID
}

type Mutation {
  createFile(name: String!, type: String!, path: String!, folderId: ID): File
}

type Query {
  getFiles(folderId: ID): [File]
  files: [File]!
  file(id: ID!): File
}
